apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: elasticsearch-ingress
  namespace: logging  #must be the same namespace where kube-prometheus stack is installed
spec:
  {{ .Values.elasticsearchIngressSpec | toYaml | nindent 2 }}